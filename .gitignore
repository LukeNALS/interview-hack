# GitNexus index (Avatar) — database local, không commit.
.gitnexus/

# Design handoff + API keys (Soniox, Claude) — tuyệt đối không commit.
init docs/

# POC artifacts (P02 bước 1 — setup chung, chạy trước P01; KHÔNG ignore toàn bộ poc/, code POC cần commit)
poc/**/node_modules
poc/**/.env
poc/**/fixtures/*.wav

# Env files app chính — giữ .env.example (không giá trị thật) để commit
.env*
!.env.example

# Next.js app (P02)
.next/
/out/
/build
node_modules/
.vercel/
next-env.d.ts
*.tsbuildinfo
coverage/
playwright-report/
test-results/

# Supabase CLI local state (không phải migrations — supabase/migrations/ vẫn commit)
supabase/.branches/
supabase/.temp/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.vercel

# Gói QA handoff (16MB audio/ảnh) — artifact kiểm thử, không vào repo
