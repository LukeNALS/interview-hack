import { AdminUsersTable } from "@/components/admin/admin-users-table";

/** Trang Users admin phase 01 — bảng user + sửa quota/plan (guard ở layout /admin). */
export default function AdminUsersPage() {
  return <AdminUsersTable />;
}
