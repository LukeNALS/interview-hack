import { CandidateStartScreen } from "@/components/candidate/candidate-start-screen";

/** /candidate — chế độ ỨNG VIÊN (plan 20260905-0820): mô tả buổi → setup → live gợi ý trả lời. */
export default function CandidatePage() {
  return <CandidateStartScreen />;
}
